const array1 = [15, 2, 7];
const array2 = [5];
const numero = 1;

// creo ciclo for del Array1
for (i = 0; i < array1.length; i++) {
  // Creo variabile di array[1]
  const incremento = array1[i]

  if (array2 <= incremento);
  // se array2 è minore di array1, aumento il suo numero
  array2.push(numero);

  console.log("secondo array:", array2);
} 
